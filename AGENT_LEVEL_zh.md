# 智能体能力层级

可以把 L1 到 L4 理解成智能体能力的四个递进层级。它不是行业统一标准，但这是工程上很实用的一套划分方式。

## L1：Prompted Assistant

本质上，这是一个带 system prompt 的大模型应用。

特点：

- 主要能力来自提示词。
- 输入一次，输出一次。
- 没有真实工具执行。
- 没有对外部环境的读写能力。

典型形态：

- 普通聊天机器人。
- 文案生成器。
- 总结、翻译、问答页面。

它能回答，但不能做事。

## L2：Tool-Using Agent

本质上，这是模型开始会调用工具的阶段。

特点：

- 模型能决定何时调用 tool。
- 有基本的 action loop：思考、调工具、继续生成。
- 可以查信息、发请求、执行函数。
- 但通常对环境状态的理解还比较弱。

典型形态：

- 会搜索网页的 AI 助手。
- 会调用数据库或 API 的客服助手。
- 会调用函数生成图表、发邮件、建日程的 agent。

它已经不只是回答问题，而是能借助工具完成任务。

## L3：State-Aware Agent

本质上，这是模型不仅会调工具，还会先理解当前环境，再做增量操作的阶段。

特点：

- 能读取当前状态再决策。
- 能基于已有上下文修改，而不是每次都全量重做。
- 强调环境一致性、对象引用和资源约束。
- 更像在操作一个真实系统，而不是只调用离散函数。

典型形态：

- 先读取当前画布，再增删改图形的 diagram agent。
- 先读当前文档，再局部改写的写作 agent。
- 先查当前任务状态，再更新任务流的 workflow agent。

它开始有世界状态模型的味道，不再只是函数路由器。

## L4：Evaluated Engineering Agent

本质上，这是 agent 已经进入工程化优化阶段。

特点：

- 有测试集、eval、scorer 或 benchmark。
- 能系统比较 prompt、tool 或策略改动前后的效果。
- 能被持续迭代，而不是只靠主观试玩判断。
- 关注稳定性、回归和成功率，而不是单次 demo 效果。

典型形态：

- 有 golden dataset 的 agent。
- 有 tool choice、structure、schema 等评分器的 agent。
- 能做 A/B 对比和回归检查的 agent workflow。

它不只是能用，而是可以被持续改进和管理。

## 一句话概括

- L1：会说。
- L2：会调工具。
- L3：会理解并操作环境状态。
- L4：会被系统化评测和迭代优化。

## 一个容易混淆的点

L1 到 L4 不完全是在说系统更聪明，而是在说它更工程化、更可执行、更可控。

很多项目模型能力并不弱，但如果没有工具、状态处理和评测体系，依然只能算低层级。

## 怎么放到项目里理解

如果把这套框架放到一个真实项目里看：

- 有 tool calling，就已经超过 L1。
- 会先读取当前状态再修改，就超过 L2。
- 有 evals 和 scorers，就接近或达到 L4。

这套划分有价值的地方在于，它把问题从“模型有多聪明”转成了“系统到底能做多少真实工作，以及我们能多大程度上持续改进和控制它”。