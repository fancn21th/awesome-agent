# RAG 能力层级

可以把 L1 到 L4 理解成 RAG 能力的四个递进层级。它不是行业统一标准，但这是一个很实用的工程化框架，用来理解检索系统是如何逐步成熟的。

这一版固定围绕 RAG 的三个核心维度来展开：

- R：Retrieval，检索
- A：Augmentation，增强
- G：Generation，生成

重点不只是说某个 level “更强”，而是看系统在这三个维度上如何随着工程化程度提升而发生变化。

## L1：Stuffed Context

本质上，这其实还不算真正的 RAG，更像是把参考资料手动塞进 prompt 的应用。

R：

- 没有真正的检索步骤。
- 上下文由人手动选择，或者由固定模板决定。
- 系统不能动态查找当前问题真正需要的知识。

A：

- 参考材料被直接复制进 prompt。
- 增强是静态的，通常对所有用户和请求都差不多。
- 几乎没有过滤、排序或按场景适配上下文的逻辑。

G：

- 模型基于塞进去的 prompt 直接生成答案。
- 输出质量高度依赖 prompt 写法，以及人工塞入的上下文质量。
- 生成结果看起来可能“知道很多”，但本质上受限于固定上下文窗口。

特点：

- 没有检索步骤。
- 系统依赖人工或固定模板决定放什么上下文。
- 只适合知识量小且变化不大的场景。

典型形态：

- 在 system prompt 里贴了一段 FAQ 的聊天机器人。
- 带少量硬编码产品说明的 demo 应用。
- 只依赖一份固定参考文档的客服助手。

它能带着额外上下文回答问题，但不能规模化访问知识。

## L2：Search-Based RAG

本质上，这是系统开始从外部知识源中检索相关信息的阶段。

R：

- 系统会从知识库中检索相关内容块。
- 检索通常由关键词搜索、向量搜索或混合搜索驱动。
- 核心目标是为当前 query 找到尽量相关的内容。

A：

- 检索到的内容块会在生成前注入到 prompt 中。
- 增强从静态变成动态。
- 系统可能开始做基础的 chunk 选择、截断和排序。

G：

- 模型基于检索到的上下文生成回答。
- 生成质量不只取决于模型本身，也取决于检索质量和 prompt 拼装方式。
- 幻觉风险会下降，但如果检索弱，答案依然会弱。

特点：

- 在生成前增加了检索步骤。
- 使用搜索、embedding 或混合检索来找到相关 chunk。
- 把检索结果注入 prompt，再进行答案生成。
- 效果高度依赖 chunking、索引方式和检索相关性。

典型形态：

- 基于向量搜索的文档问答机器人。
- 回答前先检索知识库文章的助手。
- 面向企业手册或工单系统的内部搜索助手。

到了这一层，系统不再依赖人工塞上下文，而是可以按需拉取知识。

## L3：State-Aware RAG

本质上，这是检索开始理解用户状态、任务上下文和交互历史的阶段。

R：

- 检索会依赖用户状态、会话历史、任务上下文或环境状态。
- 系统会针对不同用户、角色、任务步骤或场景采取不同检索方式。
- 检索流程中可能包含路由、过滤、重排和多阶段搜索。

A：

- 增强不再只是“把 top-k chunks 直接拼进去”。
- 系统会把检索知识与当前工作上下文、历史消息或中间结果结合起来。
- 增强开始变得更有选择性、更有结构，也更能感知当前工作流里模型已经知道什么。

G：

- 生成不仅依赖检索知识，也依赖当前状态，输出会更贴合当前场景。
- 模型可以生成与当前环境相关的答案、修改建议或动作。
- 输出不再只是通用问答，而更像面向任务的辅助。

特点：

- 检索依赖当前任务、会话、用户或环境状态。
- 系统可以把短期上下文和长期知识结合起来。
- 它会基于当前已知内容做检索优化、重排、过滤或路由。
- 它更像一个工作记忆系统，而不是单次搜索流水线。

典型形态：

- 会根据当前文件和光标位置检索代码的编程助手。
- 会根据当前草稿章节检索背景材料的写作助手。
- 会根据当前工单状态检索不同流程说明的 workflow 助手。

到了这一层，RAG 开始表现出上下文感知能力，而不再只是“搜索 + 塞 prompt”。

## L4：Evaluated Engineering RAG

本质上，这是 RAG 进入可度量、可比较、可持续优化的工程化阶段。

R：

- 检索会用 hit rate、MRR、nDCG、recall、passage relevance 等指标来衡量。
- 不同 retriever、chunking 策略、embedding 模型和 reranker 可以被系统比较。
- 检索质量不再只靠直觉判断。

A：

- 增强策略会被视为可调优的系统设计选择。
- 团队可以比较 prompt 模板、chunk 排序、引用格式、上下文压缩和 schema 结构。
- 增强会围绕 groundedness、效率和任务成功率来优化。

G：

- 生成会按 factuality、grounding、usefulness、task success 和 regression risk 等维度进行评估。
- 输出质量会通过 eval 集、评分器或业务指标来验证。
- 生成成为持续工程迭代的一环，而不是一次性的 demo 层。

特点：

- 有 eval 集、检索指标、答案质量评分器或业务成功指标。
- 可以系统比较 chunking、embedding、retriever、reranker 和 prompting 策略。
- 能追踪检索质量、grounding、引用准确性和任务成功率上的回归。
- 优化依赖系统化迭代，而不是主观试玩。

典型形态：

- 有 golden question set 和预期支持段落的 RAG 系统。
- 带 hit-rate、MRR、nDCG 或 groundedness 评估的检索流水线。
- 能对不同检索和生成策略做 A/B 测试的生产工作流。

它不只是一个 RAG demo，而是一个可以像真实基础设施一样被运营和持续改进的系统。

## 一句话概括

- L1：R 是手动的，A 是静态的，G 被 prompt 绑定。
- L2：R 变得可搜索，A 变得动态，G 变得基于检索结果。
- L3：R 变得状态感知，A 变得上下文感知，G 变得任务感知。
- L4：R、A、G 三者都变得可度量、可持续优化。

## 一个容易混淆的点

RAG 的成熟度不只是更强的模型或更大的向量库，而是检索如何变得更准，增强如何变得更有选择和结构，生成如何变得更可溯源、更可验证。

很多团队说自己做了 RAG，但实际上还停留在 L1 或较弱的 L2。只要检索还不具备状态感知，也没有评测体系，这个系统就仍然处在比较早期的阶段。

## 怎么放到项目里理解

如果把这套框架放到一个真实项目里看：

- 如果 R 还是手动的，A 还是固定的，G 只是基于塞进去的 prompt 工作，那它是 L1。
- 如果 R 能从知识库中搜索，A 能动态插入检索结果，那它至少是 L2。
- 如果 R 会随状态变化，A 会随工作流上下文调整，那它是 L3。
- 如果 R、A、G 三个维度都能用指标或 evals 评估并优化，那它就接近或达到 L4。

这套划分有价值的地方在于，它把问题从“我们有没有 RAG”转成了“随着系统变得更稳健、更理解上下文、更容易优化，Retrieval、Augmentation、Generation 三个维度分别是怎么演化的”。